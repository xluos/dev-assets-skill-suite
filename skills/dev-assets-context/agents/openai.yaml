interface:
  display_name: "Dev Assets Context"
  short_description: "读取并刷新当前分支的开发记忆，恢复默认上下文入口"
  default_prompt: "Use $dev-assets-context to orient the current branch's saved development memory, identify missing materials, and recover context before coding."
