{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "npx dev-assets hook session-start"
          }
        ],
        "description": "Load repo+branch dev-assets context at session start",
        "id": "dev-assets:session-start"
      }
    ],
    "PreCompact": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "npx dev-assets hook pre-compact",
            "timeout": 20
          }
        ],
        "description": "Refresh working-tree-derived navigation before compaction",
        "id": "dev-assets:pre-compact"
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "npx dev-assets hook stop",
            "timeout": 15
          }
        ],
        "description": "Persist lightweight HEAD markers after each response",
        "id": "dev-assets:stop"
      }
    ],
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "npx dev-assets hook session-end",
            "timeout": 15
          }
        ],
        "description": "Persist final HEAD marker at session end",
        "id": "dev-assets:session-end"
      }
    ]
  }
}
